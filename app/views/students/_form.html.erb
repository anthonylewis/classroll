<%= form_for(@student) do |f| %>
  <% if @student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student.errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
      <% @student.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <b><%= f.label :name %></b><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <b><%= f.label :email %></b><br />
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <b><%= f.label :url %></b> (optional)<br />
    <%= f.text_field :url %>
  </div>
  <div class="field">
    <b><%= f.label :bio %></b> (optional)<br />
    <%= f.text_area :bio %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
